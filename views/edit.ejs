<!doctype html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
  <div class="w-full bg-zinc-900 min-h-screen p-10 text-white">
    <div class="w-full flex justify-end">
        <a class="bg-red-700 rounded-md px-3 py-2 inline-block mb-5 text-sm" href="/logout">Logout</a>
    </div>
    
    <h3 class="text-3xl"> <span class="font-light">Hey,</h3>
    <h5 class="mb-5">You can edit your post here.</h5>

    <form action="/update/<%= post._id %>" method="post">
        <textarea placeholder="What's on your mind?" class="p-3 outline-none w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md" name="content">
            <%= post.content  %>
        </textarea>
        <input type="submit" class="text-sm px-3 py-2 w-25 bg-blue-500 rounded-md block mt-2" value="Edit New Post">
    </form>

  </div>
</body>
</html>